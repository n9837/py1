print("x y z w    f")
for x in (True, False):
    for y in (True, False):
        for z in (True, False):
            for w in (True, False):
                p = x and (not y)
                q = (not z) or (not w)
                e = (not w) or x
                f = (not p or q) and (e or y)
            if f == 0:
                print(int(x), int(y), int(z), int(w), "  ", int(f))
